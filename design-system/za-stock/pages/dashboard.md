# Dashboard / app pages override

Extends `../MASTER.md`.

## Layout

- Shell: sticky sidebar (`lg:w-60`), max content width `max-w-7xl`
- Stock: KPI strip (2 metrics, divide-x) above table — not nested cards
- Tables: sticky header, row hover via `bg-secondary`, skeleton bars while loading
- New invoice: sticky totals panel on desktop (`lg:sticky lg:top-6`)

## Spacing

- Page gap: `mt-6` after header
- Row padding: `px-5 py-4`
- Controls: `rounded-lg`, `py-3`
